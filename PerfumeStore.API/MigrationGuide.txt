For Server:
project > server
startup > server
cli:

AspIdentity tables  - do it at begining
    1. Add-Migration AspIdentityUser -Context ApplicationDbContext
    2. Update-Database -Context ApplicationDbContext  
DUENDE
    1. Add-Migration InitialIdentityServerMigration -c PersistedGrantDbContext
    2. Add-Migration InitialIdentityServerMigration -c ConfigurationDbContext
    3. Update-database -Context PersistedGrantDbContext
    4. Update-database -Context ConfigurationDbContext


----------------------------------------------------------------------------------------------------------------
For API:
project > domain
startup > API
cli: 
    1. Add-Migration InitialPerfumeShopMigration -context ShopDbContext
    2. Update-Database -context ShopDbContext

INSERTS 

insert into dbo.ProductCategories(Id, Name) values (3, 'catc');

SET IDENTITY_INSERT dbo.ProductCategories ON
